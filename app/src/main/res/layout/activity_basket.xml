<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.dev.a4.www.database.Basket">

    <Button
        android:id="@+id/btnorder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="Заказать"
        android:backgroundTint="@android:color/holo_green_light"
        android:textColor="@android:color/background_light"
        android:fontFamily="serif"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/sum"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnorder"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:text="Сумма:"
        android:textColor="@android:color/holo_green_light"
        android:fontFamily="serif"
        android:textStyle="bold"
        android:textSize="@dimen/standard_text"
        android:background="@drawable/dish_design"
        android:layout_margin="@dimen/description_blank_margin"
        android:paddingLeft="@dimen/description_blank_margin" />

    <ListView
        android:id="@+id/orderlist"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/sum"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:divider="@drawable/customdivider"
        android:layout_margin="@dimen/description_blank_margin" />

    <Button
        android:id="@+id/btnend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="Подтвердить"
        android:backgroundTint="@android:color/holo_green_light"
        android:textColor="@android:color/background_light"
        android:fontFamily="serif"
        android:textStyle="bold"
        android:textSize="@dimen/standard_text" />

</RelativeLayout>
